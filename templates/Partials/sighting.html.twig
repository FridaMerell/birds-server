<div class="list-item">
	<span>
		{{ sighting.user.email }}
	</span>
	<span>
		{{ sighting.species }}
	</span>

	<span class="links">
		<a href="{{ path('app_sighting_show', {'id': sighting.id}) }}" class="arrow">Visa</a>
	</span>
</div>